# TODO: Connect DB to Products Page (index.html)

## Steps to Complete
- [x] Update `frontend/assets/js/main.js` to fetch products from `../backend/products.php` instead of using hardcoded data.
- [x] Map backend fields (id_produit -> id, nom_produit -> name, prix -> price, description -> description).
- [x] Add error handling for fetch requests.
- [x] Test the connection by loading the frontend page (PHP server running on localhost:8082).
